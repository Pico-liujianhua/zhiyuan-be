<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tongji.auth.mapper.UserOAuthConnectionMapper">

    <resultMap id="ConnectionResultMap" type="com.tongji.auth.model.UserOAuthConnection">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="provider" property="provider"/>
        <result column="open_id" property="openId"/>
        <result column="union_id" property="unionId"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar" property="avatar"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findByProviderAndOpenId" resultMap="ConnectionResultMap">
        SELECT *
        FROM user_oauth_connections
        WHERE provider = #{provider}
          AND open_id = #{openId}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.tongji.auth.model.UserOAuthConnection"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_oauth_connections (
            user_id,
            provider,
            open_id,
            union_id,
            nickname,
            avatar,
            created_at,
            updated_at
        ) VALUES (
            #{userId},
            #{provider},
            #{openId},
            #{unionId},
            #{nickname},
            #{avatar},
            #{createdAt},
            #{updatedAt}
        )
    </insert>
</mapper>
